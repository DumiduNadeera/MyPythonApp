class car:

	def car(self, self.brand, self.speed):
		self.brand = brand
		self.speed = speed

	def display(self):
		printf("Car is {self.brand} & speed {self.speed}")

	def accelarate(self,speed):
		self.speed = self.speed + speed

my_car = car("Toyota", 60)
mycar.display()
				
